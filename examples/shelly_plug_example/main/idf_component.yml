version: "1.0.2"
dependencies:
    idf: ">=5.5.0"
    sachin42/httpclient: ^1.0.1
    elrebo-de/shelly_plug:
      version: "1.0.2"
      override_path: '../../../' # three levels up, pointing to the directory with the component itself
